import {StyleSheet} from 'react-native';

export const _styles = StyleSheet.create({
  allCenter: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  flexRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
